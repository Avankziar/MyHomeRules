Future Featurs:
- Bungee wie Spigot MysqlSetup connection und Reconnection treiber updaten, siehe Scc

Build 6-2-0
- MC 1.19.2 support+
- IFH Administration implementiert
- MysqlSetup aktualisiert
- Tabellen name MyHomeRulesPlayerData in mhrPlayerData geändert. Bitte bei euch auch ändern.

Build 6-1-x ================================================================================================================
+ MC 1.19 support

Build 6 =====================================================================================================================
+ Java 17 und MC 1.18.1 support
+ Mysql Driver Update 
  
Build 5 ========================================================================================================================
- +Eine seitenbasierte Ansicht für die Joinnachrichten, die per Configboolean eingeschaltet werden kann.
- +Spieler haben nun einen boolean für das Wiederrufen der Regeln. Der beim Wiederrufen auf true gesetzt wird. Dann wird dieser Spieler
   in der liste angezeigt.
- +Spieler können nun als "gelöscht" getrackt werden per Command.
- +neue Befehle:
    - +/rule [Zahl] | Zeigt pro Seite die Serverregeln an.
    - +/rules delete <Spielernamen> | Trackt den Spieler als "aus dem System gelöscht".
    - +/rules site [Zahl] | Springt zu der angegeben Seite, im Seitenbasierten Modus.
    - +/rules todeletelist | Zeigt alle Spieler an, welche die Regeln wiederrufen haben.
- +Support Metrics
- +Support InterfaceHub
- +Mysql wurde erweitert:
   ALTER TABLE `YourDateTable` ADD `revoked` TINYINT(1) NOT NULL DEFAULT '0' AFTER `datetime` 
   AND `deleted` TINYINT(1) NOT NULL DEFAULT '0' AFTER `revoked`;
  
Build 4 =======================================================================================================================
- CommandTree und YamlManager eingefügt
- Beim akzeptieren und wiederrufen, können nun Befehle ausgeführt werden.